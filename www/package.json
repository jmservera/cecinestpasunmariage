{
  "scripts": {
    "start": "bash start",
    "clean": "rm -rf public",
    "build": "npm run clean && hugo --gc --minify"
  },
  "devDependencies": {
    "@azure/static-web-apps-cli": "^1.1.6"
  }
}