{
  "scripts": {
    "start": "bash start",
    "clean": "rm -rf public",
    "build": "npm run clean && hugo --gc --minify",
    "test": "prettier --check . ",
    "format": "prettier --write . "
  },
  "devDependencies": {
    "@azure/static-web-apps-cli": "^1.1.8",
    "@types/jquery": "^3.5.30",
    "prettier": "^3.2.5",
    "prettier-plugin-go-template": "0.0.15"
  }
}
